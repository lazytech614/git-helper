import { GlossaryTerm } from "../types";

export const indexingTerm: GlossaryTerm = {
  id: "indexing",
  term: "Indexing",
  definition:
    "A data structure technique (commonly B-trees or hash tables) that improves the speed of data retrieval on a database table, at the cost of additional storage and slower writes.",
  category: "database",
  related: ["join"],
};
