import { GlossaryTerm } from "../types";

export const corsTerm: GlossaryTerm = {
  id: "cors",
  term: "CORS",
  definition:
    "Cross-Origin Resource Sharing. A browser security mechanism that uses HTTP headers to control whether a web page from one origin can request resources from a server on a different origin.",
  category: "networking",
  related: ["rest"],
};
